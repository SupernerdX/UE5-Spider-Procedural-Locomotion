# Auto detect text files and perform LF normalization
* text=auto

# Unreal binary assets are large and cannot be usefully merged as text.
*.uasset filter=lfs diff=lfs merge=lfs -text
*.umap filter=lfs diff=lfs merge=lfs -text
